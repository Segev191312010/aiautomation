# Session Handoff — 2026-03-29

## Status
- **Branch**: master (all work uncommitted)
- **Commits this session**: 0 new commits
- **Uncommitted changes**: 120 files changed, +4663/-37342 lines
- **Backend tests**: 301 passed, 63 failed (pre-existing), 28 errors (pre-existing)
- **Frontend tests**: 81/81 passed
- **Frontend typecheck**: passing
- **Frontend build**: passing

## What's Done

### Stage 9: Trade Truth Layer (COMPLETE)
- 13 canonical outcome fields on Trade model (mode, position_id, entry_price, exit_price, realized_pnl, etc.)
- `finalize_trade_outcome()` with side-aware P&L (position_side param)
- `persist_rule_revision()` atomic version bumping
- `trade_utils.py` shared outcome extractor (canonical-first, metadata fallback)
- All entry writers normalized (bot_runner, direct_ai_trader, order_executor)
- All exit writers use finalize_trade_outcome instead of manual metadata["pnl"]
- Validation engine rewrite: canonical-first matching, evidence details persistence
- Performance ledger, ai_learning, ai_guardrails, portfolio_analytics all converged on trade_utils
- API contracts + TS types + UI evidence display updated
- 18 S9 tests + 3 E2E tests

### Stage 10: AI Decision Ledger (COMPLETE)
- 4 new DB tables: ai_decision_runs, ai_decision_items, ai_evaluation_runs, ai_evaluation_slices
- `ai_decision_ledger.py`: full run/item lifecycle CRUD
- Optimizer instrumented: every cycle creates decision_run + items, marks applied/blocked/shadow
- Guardrails: execute_with_audit, log_ai_action, log_shadow_decision all accept decision IDs
- Rule lab: AI-created rules stamped with origin_decision_id/origin_run_id
- Trade finalization: attach_realized_trade auto-links when decision_id present
- `ai_replay.py`: stored_context_existing, stored_context_generate, rule_backtest_replay
- `ai_evaluator.py`: sliced metrics (overall/symbol/regime/confidence/action_type)
- `ai_learning.py`: ledger-backed evaluate_past_decisions, model-aware cost reporting
- 8 new API endpoints on /api/autopilot/
- Evaluation tab in AutopilotPage
- 25 S10 tests + 3 E2E tests

### Stage 10 Wave 1: Correctness Extraction (COMPLETE)
- `evaluation_math.py`: 10 pure metric helpers extracted from ai_evaluator
- `context_utils.py`: stable JSON, volatile-field stripping, deterministic hashing
- `candidate_registry.py`: resolve/generate candidates, centralized LLM call
- `rule_replay_adapter.py`: is_rule_replayable() fail-closed guard, build_backtest_request
- `replay_scoring.py`: centralized scoring policy by item type
- Replay semantics split: stored_context_existing (no LLM) vs stored_context_generate (LLM via registry)
- 45 new Wave 1 tests

### Stage 0: Baseline & Truth (COMPLETE)
- README rewritten, docs/architecture.md + docs/baseline.md created
- Analytics mock deception removed (DegradedStateCard, per-section status)
- .gitignore fixed, scratch files deleted
- Exception hotspot inventory created
- Analytics api.ts mapping functions added (fetchPortfolioAnalytics, fetchRiskLimits, etc.)

### Stage 1A: Router Extraction (BATCH A DONE)
- 5 routers extracted: auth, settings, backtest, sectors, events
- `runtime_state.py` created (initialize/reset/get API)
- `routers/__init__.py` with register_routers()
- Route parity verified: 174/174

### Other
- ib_chart integrated as sidecar (Charts page, multi-chart screener button)
- IBKR port config (.env: 7496 for TWS)

## What's In Progress
- Stage 1A Batch B-F: remaining route extraction from main.py (76 routes still there)

## Known Bugs (from comprehensive review)

### CRITICAL
1. Direct AI SELL bypasses exit lifecycle — no finalize_trade_outcome, position not cleared
2. Risk check runs before dynamic sizing — guards evaluate wrong quantity
3. Legacy fallback can auto-promote paper rules on non-canonical evidence

### HIGH
4. AI direct trade concentration check fails open on exception
5. SQLite PRAGMA foreign_keys never enabled — CASCADE is dead
6. Decision items marked "applied" when queued, not when executed

### MEDIUM
7. Audit log decision lineage stripped from API response (missing from contract)
8. Replay contract/resolver/frontend type mismatch
9. Bot health staleness uses WS threshold (10s) not bot interval (900s)

## Key Decisions Made
- Stage 9 Trade model uses Optional fields for backward compat (old JSON deserializes)
- `finalize_trade_outcome` takes `position_side` param (not inferred from trade.action)
- `persist_rule_revision` is atomic (single DB transaction)
- Replay v1: stored-context replay + deterministic scoring, not bar-by-bar LLM
- Rule replay requires explicit replay_config (fail-closed)
- Stage 1A: router extraction only, defer database/model split to 1B/1C
- Shared runtime state in runtime_state.py module, not router globals

## Files Touched (key files only)
- `backend/models.py` — S9 Trade fields + S10 Rule origin fields + replay_config
- `backend/database.py` — finalize_trade_outcome, persist_rule_revision, S10 tables, get_trade
- `backend/ai_decision_ledger.py` — NEW: decision run/item lifecycle
- `backend/ai_evaluator.py` — NEW: evaluation engine, slimmed to orchestration
- `backend/ai_replay.py` — NEW: replay engine (3 modes)
- `backend/evaluation_math.py` — NEW: pure metric formulas
- `backend/context_utils.py` — NEW: stable hashing
- `backend/candidate_registry.py` — NEW: candidate resolution/generation
- `backend/rule_replay_adapter.py` — NEW: replayability guard
- `backend/replay_scoring.py` — NEW: scoring policy
- `backend/trade_utils.py` — NEW: shared outcome extractor
- `backend/runtime_state.py` — NEW: shared runtime state
- `backend/routers/` — NEW: 5 extracted routers (Batch A)
- `backend/main.py` — route extraction started, router registration added
- `dashboard/src/pages/AutopilotPage.tsx` — evaluation tab
- `dashboard/src/pages/ChartsPage.tsx` — NEW: ib_chart integration
- `dashboard/src/pages/AnalyticsPage.tsx` — mock removal, degraded UI
- `dashboard/src/types/index.ts` — S9 Trade fields, AppRoute
- `dashboard/src/types/advisor.ts` — S10 types
- `dashboard/src/services/api.ts` — S10 client + analytics mapping

## Gotchas for Next Session
- The 63 failed backend tests are PRE-EXISTING (stock_profile, websocket, alerts, diagnostics) — not regressions
- `ib_chart/` is a git-cloned subdir, not a submodule — runs on port 5001 with IB_PORT=7496
- `.env` has IBKR_PORT=7496 (TWS paper). Change to 4001 for Gateway live.
- Route parity snapshot is at `backend/.tmp/routes_before.txt`

## Resume Command
> Continue Stage 1A router extraction. Batch A (auth, settings, backtest, sectors, events) is done with route parity verified. Next: Batch B — extract screener, alerts, rules CRUD, orders, and bot routes. Then fix the 3 CRITICAL bugs before shipping.
