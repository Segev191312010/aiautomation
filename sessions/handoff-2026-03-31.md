# Session Handoff — 2026-03-31

## Status
- **Branch**: master
- **Commits this session**: 4
- **Uncommitted changes**: ~15 dirty files from prior sessions (not this session)
- **Tests**: backend 435 passed / frontend 81 passed / typecheck clean

## What's Done

### Commit 1: `65f897d` — Test suite fix + HB1 hotfixes
- Installed missing `pytest-asyncio` (was in requirements.txt but not in venv)
- Rewrote `tests/conftest.py`: repo-local `.tmp/pytest` temp root, autouse `_restore_db_path` fixture
- **HB1-01**: Fixed `NameError: has_existing_position` in live AI BUY path. Added `register_entry_position_from_fill()` for lifecycle parity. Fixed `skip_safety=False` to `True`.
- **HB1-03**: Added regression test for non-canonical promotion rejection.
- **HB1-04**: Updated test to assert fail-closed behavior (`allowed=False, reason=error_blocked`).
- **HB1-05/06/02**: Verified already correct — FK enforcement, pending/applied truth, sizing order. **Re-verified 2026-04-09 (Phase 1) with regression tests after db/ split**: `tests/test_execution_brain.py::test_hb1_02_risk_check_runs_after_dynamic_sizing`, `tests/test_ai_decision_ledger.py::test_hb1_05_decision_run_delete_cascades_to_items`, `tests/test_direct_ai_trader.py::test_hb1_06_failed_live_trade_does_not_mark_decision_item_applied` (+ positive companion).
- Fixed `test_execution_brain.py` import drift (`_pending_direct_candidates` to `_pending_direct_queue`).

### Commit 2: `5cd4391` — First pass exception hardening
- Replaced 7 bare `except Exception: pass` in `bot_runner.py` and `main.py` with logged handlers.

### Commit 3: `055d063` — Cold-start conftest, filter policy, contract tests, full exception cleanup
- **conftest cold-start fix**: Removed `tmp_path_factory.getbasetemp()` which hit unwritable global temp on Windows cold starts. Uses `pathlib.Path` directly now. Verified with 2 cold-start runs.
- **rule_backtest filter policy**: Added `model_validator` on `ReplayRequest` that rejects `min_confidence`, `symbols`, `action_types` with 422 for `rule_backtest` mode. Was silently ignoring them.
- **Contract tests**: Added `test_evaluation_run_error_field_persists` and `test_evaluation_run_summary_carries_metadata` (runs_evaluated, filters_applied round-trip). Economic `data_quality`/`metric_source` already covered by existing tests.
- **Exception cleanup**: Zero bare `except Exception:` remaining across 8 critical files (was 21). All now log with `as exc` or use specific types like `json.JSONDecodeError`.

### Already done before this session (commit `926b608`)
- stored_context_generate filter parity (min_confidence, symbols, action_types applied in ai_replay.py)
- Evaluation metadata preservation (EvaluationRunResponse carries error, runs_evaluated, filters_applied)
- Economic report data_quality and metric_source threading

## What's Not Done

### Still open
- **~15 pre-existing dirty files**: `bot_runner.py`, `config.py`, `context_builder.py`, `database.py`, `execution_brain.py`, `ibkr_client.py`, `order_executor.py`, `risk_manager.py`, `safety_kernel.py`, and several session/review docs. These were dirty at session start. Need investigation — likely from prior session work that was never committed.
- **Stage 2 bugs**: 10 confirmed bugs from parallel review agents (see `memory/project_stage2_bugs.md`)
- **baseline_key underused**: Accepted and stored in replay requests but doesn't affect replay behavior. Contract-truth gap, lower priority.

### Deprioritized (not needed now)
- Monolith splitting (database.py, bot_runner.py, main.py) — maintainability, not safety
- DuckDB layer — not needed at 534-symbol scale
- Frontend performance (React.memo, code splitting) — no reported UI issues
- CI/CD, Prometheus, runbooks — single-operator setup

## Key Decisions Made
- **conftest must never call getbasetemp()** — it creates dirs in the global temp root which is unwritable on Windows cold starts
- **rule_backtest should reject filters at the model level** (422), not silently ignore them — chose Pydantic model_validator over route-level check
- **JSON parse fallbacks should catch (JSONDecodeError, ValueError)** not broad Exception — more precise, still handles all realistic cases
- **skip_safety=True is correct** for direct AI trades — safety already evaluated upstream by safety_gate

## Files Touched (This Session)
- `backend/tests/conftest.py` — cold-start safe tmp_path, DB_PATH restore
- `backend/api_contracts.py` — ReplayRequest model_validator for rule_backtest
- `backend/direct_ai_trader.py` — HB1-01 lifecycle, NameError, skip_safety, exception logging
- `backend/bot_runner.py` — bare except cleanup
- `backend/main.py` — bare except cleanup (websocket, yfinance, coinbase)
- `backend/ai_optimizer.py` — bare except cleanup
- `backend/ai_learning.py` — bare except cleanup
- `backend/ai_replay.py` — bare except cleanup
- `backend/ai_guardrails.py` — bare except cleanup
- `backend/ai_evaluator.py` — bare except to specific JSONDecodeError
- `backend/tests/test_direct_ai_trader.py` — register_entry mock
- `backend/tests/test_execution_brain.py` — import fix
- `backend/tests/test_portfolio_impact.py` — HB1-04 assertion
- `backend/tests/test_rule_validation.py` — HB1-03 rejection test
- `backend/tests/test_ai_replay.py` — rule_backtest filter rejection test
- `backend/tests/test_ai_evaluator.py` — error + metadata contract tests

## Quality Gate Results (End of Session)
```
Backend:   435 passed, 0 failures (cold-start verified)
Frontend:  81 passed (vitest)
Typecheck: clean (tsc --noEmit)
```

## Resume Command
> Continue on master. Stage 0 stabilization complete (435 green, cold-start safe, 0 bare excepts, rule_backtest filter policy enforced). Next priorities: (1) investigate ~15 pre-existing dirty files to commit or discard, (2) fix 10 Stage 2 bugs from project_stage2_bugs.md, (3) resolve baseline_key contract gap in replay.
