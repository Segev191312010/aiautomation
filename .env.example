# aiautomation dev env — copy to .env.local and fill in.
#
# Bootstrap secret for the loopback dev auth flow. Must match the backend's
# JWT_BOOTSTRAP_SECRET (see backend/.env). The token endpoint is loopback-only
# and refuses LIVE mode server-side, so this is safe for local/personal use.
VITE_JWT_BOOTSTRAP_SECRET=
